#include<stdio.h>
int length(int x)
{
	int arr[100];
	int s = 0;
	int w = 0;
	scanf("%d %d", s, w);
	if (s > w)
	{
		printf("")
	}
}
int main()
{
	int lg = 0;
	length(lg);
	printf("³¤¶ÈÎª%d", lg);
	return 0;
}